export type SampleLabel = {
    id: number;
    name: string;
}

export type User = {
    id?: number;
    mobile: string;
    password?: string;
    email?: string;
    username?: string;
    avatar?: string;
}